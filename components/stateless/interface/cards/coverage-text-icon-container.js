import { Box } from "@mui/material";

const CoverageTextIconContainer = ({ children }) => {
  return (
    <Box mr={3} display="flex" alignItems="center">
      {children}
    </Box>
  );
};

export default CoverageTextIconContainer;
